(defvar *a* (make-array '10))
(defun creat (a) 
  (dotimes (i 10)
    (setf (aref a i) (+ i 1))
  )
)
(defun dump (l)
  (dotimes (i 10)
    (format t "[~2,'0d]:~8,'0d~%" i (aref l i))
  )
)
(creat *a*)
(dump *a*)